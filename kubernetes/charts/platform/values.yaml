# Platform chart defaults
# Each platform service overrides these in its platform.yaml

name: ""
host: ""
port: 80
private: true

storage: {}
# storage:
#   size: 1Gi

# Infrastructure settings (injected by Ansible at deploy time).
#
# publicTarget is the hostname/IP that public IngressRoutes resolve to via
# external-dns. Post-cloudflared this is the tunnel CNAME, not a host IP.
infrastructure:
  tailscaleHostname: ""
  publicTarget: ""
  nodeName: ""
