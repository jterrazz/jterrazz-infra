name: portainer
host: portainer.jterrazz.com
port: 9000
private: true
storage:
  # Named-volume map (platform chart 2.0). Key `data` keeps the
  # historical PV/PVC names portainer-data and the hostPath
  # /var/lib/k8s-data/portainer unchanged.
  data:
    size: 1Gi
