---
# SigNoz - Observability Platform
# APM, Metrics, Logs, Traces
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: signoz
  namespace: platform-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://charts.signoz.io
    chart: signoz
    targetRevision: 0.105.2
    helm:
      values: |
        global:
          storageClass: manual

        # ClickHouse - Data storage (uses ~860MB baseline)
        clickhouse:
          persistence:
            enabled: true
            size: 10Gi
          resources:
            requests:
              cpu: 200m
              memory: 1Gi
            limits:
              memory: 1536Mi
          # Zookeeper is under clickhouse
          zookeeper:
            image:
              registry: docker.io
              repository: signoz/zookeeper
              tag: "3.7.1"
            persistence:
              enabled: true
              size: 1Gi
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 512Mi

        # Query Service (signoz-0 pod, uses ~175MB)
        queryService:
          resources:
            requests:
              cpu: 50m
              memory: 200Mi
            limits:
              memory: 400Mi

        # Frontend (embedded in queryService in v0.105+)
        frontend:
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi

        # Alert Manager
        alertmanager:
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi

        # OpenTelemetry Collector (uses ~185MB)
        otelCollector:
          resources:
            requests:
              cpu: 50m
              memory: 200Mi
            limits:
              memory: 300Mi

  destination:
    server: https://kubernetes.default.svc
    namespace: platform-observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
