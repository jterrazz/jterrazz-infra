---
# n8n PersistentVolumeClaim
# Pre-created for use with existingClaim in n8n helm chart
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data
  namespace: platform-automation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
