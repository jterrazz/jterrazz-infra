# VictoriaMetrics single-node — the metrics store, replacing Prometheus.
#
# Chart: vm/victoria-metrics-single (see platform_chart_versions.victoria_metrics).
# Two ingest paths, both on :8428 —
#   * it SCRAPES, using `-promscrape.config` (server.scrape below), which is a
#     Prometheus-compatible scrape config: the same kubernetes_sd jobs the
#     prometheus chart shipped;
#   * it RECEIVES Prometheus remote-write at /api/v1/write from the OTel
#     collector. Unlike Prometheus, no `web.enable-remote-write-receiver`
#     equivalent exists — the path is always on.
#     https://docs.victoriametrics.com/victoriametrics/url-examples/#apiv1write
#
# All three Victoria charts ship the server as a StatefulSet whose Service is
# HEADLESS (`clusterIP: None`) — it doubles as the StatefulSet's governing
# service. Unlike prometheus/loki/tempo, `kubectl get svc` shows no ClusterIP;
# the name still resolves, to the pod IP directly. Left at the chart default:
# giving it a ClusterIP would break the per-pod DNS a StatefulSet guarantees.

server:
  # Without this the objects are named <release>-victoria-metrics-single-server.
  # Every consumer (Grafana datasource, the collector's remote-write exporter)
  # addresses the Service by name, so keep it short and stable.
  fullnameOverride: victoria-metrics

  # DATA LOSS IF CHANGED. Names the PVC that kubernetes/services/victoria-metrics/
  # platform.yaml creates (<name>-<key> = victoria-metrics-data) over a hostPath
  # PV at /var/lib/k8s-data/victoria-metrics.
  #
  # `existingClaim` is load-bearing beyond the name: this chart runs the server
  # as a StatefulSet, and setting existingClaim makes the template drop its
  # volumeClaimTemplate entirely and mount the named PVC instead. Unset it and
  # the StatefulSet provisions server-volume-victoria-metrics-0 from the default
  # StorageClass and starts on an empty TSDB.
  persistentVolume:
    existingClaim: victoria-metrics-data

  # Age-based only, matching what Prometheus kept. Do NOT add
  # `-retention.maxDiskSpaceUsageBytes`: a size cap silently clips the window
  # (the same trap the prometheus `retentionSize` had — a 2GB cap gave ~7d).
  retentionPeriod: "30d"

  resources:
    requests:
      cpu: 50m
      memory: 640Mi
    limits:
      memory: 896Mi

  extraArgs:
    # The Victoria-family equivalent of the GOMEMLIMIT this repo set on every
    # Go telemetry component. VictoriaMetrics does not size its caches from the
    # Go heap; it budgets them from `-memory.allowedPercent` of the detected
    # cgroup memory limit (i.e. of resources.limits.memory above), and shrinks
    # them under pressure. Written out explicitly even though 60 is the upstream
    # default: this is a deliberate bound, and a percentage tracks the limit
    # instead of going stale the way an absolute `-memory.allowedBytes` would.
    memory.allowedPercent: 60

  # Reproduces the scrape surface of prometheus-community/prometheus 29.19.0's
  # default `serverFiles."prometheus.yml"`, which is what this cluster scraped
  # until the migration. Rendering that chart against the old values file is how
  # this was derived — it is not a hand-written approximation.
  #
  # Enabling this also makes the chart render the ClusterRole/Binding that
  # kubernetes_sd needs (pods, services, endpointslices, nodes, nodes/metrics,
  # ingresses, /metrics) and mount the config at /scrapeconfig/scrape.yml.
  #
  # DROPPED from the upstream set, deliberately, because both scraped exactly
  # nothing here:
  #   * `kubernetes-services`  — blackbox-exporter probe job. It keeps only
  #     Services carrying `prometheus.io/probe`, of which this cluster has none,
  #     and its targets are rewritten to the address `blackbox`, which does not
  #     resolve. Dead config.
  #   * `prometheus-pushgateway` — the pushgateway subchart is disabled (it was
  #     `prometheus-pushgateway.enabled: false` before too).
  # The `*-slow` variants are KEPT even though nothing sets
  # `prometheus.io/scrape_slow` today: they are half of the annotation contract
  # an app opts into, and the `drop` rules in the fast jobs reference them.
  scrape:
    enabled: true
    config:
      global:
        # 1m, not the chart's 15s default — Prometheus scraped at 1m here and a
        # 4x resolution bump is a 4x sample-rate bump on a memory-bound node.
        scrape_interval: 1m
        scrape_timeout: 10s

      scrape_configs:
        # VictoriaMetrics' own metrics, on the port it listens on. (The upstream
        # prometheus chart's equivalent job was `prometheus` -> localhost:9090.)
        - job_name: victoriametrics
          static_configs:
            - targets: ["localhost:8428"]

        - job_name: kubernetes-api-servers
          bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
          kubernetes_sd_configs:
            - role: endpointslice
          relabel_configs:
            - action: keep
              regex: default;kubernetes;https
              source_labels:
                - __meta_kubernetes_namespace
                - __meta_kubernetes_service_name
                - __meta_kubernetes_endpointslice_port_name
          scheme: https
          tls_config:
            ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          # CARDINALITY. These three histograms were ~42k active series between
          # them — about 37% of the whole TSDB — on a node whose entire metrics
          # budget is 896Mi. They are per-(verb × resource × subresource ×
          # scope × le) buckets, so they grow with the API surface rather than
          # with anything this cluster does, and NOTHING reads them: neither
          # dashboard in kubernetes/services/grafana/dashboards/ references
          # them, and no app ships an alert rule that does.
          #
          # Dropped at INGEST (metric_relabel_configs), not by a query rule —
          # a series that is never stored costs no index, no cache and no disk.
          # The rest of the apiserver job is kept: request rates, error rates
          # and the _count/_sum of these same histograms all survive, because
          # only the `_bucket` series are matched. That leaves averages
          # computable and only the quantiles gone.
          #
          # Re-adding one means measuring first: check the series count with
          #   /api/v1/status/tsdb  (topSeriesCountByMetricName)
          # before assuming it is affordable now.
          metric_relabel_configs:
            - action: drop
              source_labels:
                - __name__
              regex: apiserver_request_duration_seconds_bucket|apiserver_request_sli_duration_seconds_bucket|etcd_request_duration_seconds_bucket

        - job_name: kubernetes-nodes
          bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
          kubernetes_sd_configs:
            - role: node
          relabel_configs:
            - action: labelmap
              regex: __meta_kubernetes_node_label_(.+)
          scheme: https
          tls_config:
            ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
            insecure_skip_verify: true

        # cadvisor. Both Grafana dashboards depend on this job:
        # container_cpu_usage_seconds_total / container_memory_working_set_bytes
        # / container_network_*_bytes_total come from nowhere else.
        - job_name: kubernetes-nodes-cadvisor
          bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
          kubernetes_sd_configs:
            - role: node
          metrics_path: /metrics/cadvisor
          relabel_configs:
            - action: labelmap
              regex: __meta_kubernetes_node_label_(.+)
            - source_labels:
                - __metrics_path__
              target_label: metrics_path
          scheme: https
          tls_config:
            ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
            insecure_skip_verify: true

        - job_name: kubernetes-pods
          honor_labels: true
          kubernetes_sd_configs:
            - role: pod
          relabel_configs:
            - action: keep
              regex: true
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_scrape
            - action: drop
              regex: true
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_scrape_slow
            - action: replace
              regex: (https?)
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_scheme
              target_label: __scheme__
            - action: replace
              regex: (.+)
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_path
              target_label: __metrics_path__
            - action: replace
              regex: (\d+);(([A-Fa-f0-9]{1,4}::?){1,7}[A-Fa-f0-9]{1,4})
              replacement: "[$2]:$1"
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_port
                - __meta_kubernetes_pod_ip
              target_label: __address__
            - action: replace
              regex: (\d+);((([0-9]+?)(\.|$)){4})
              replacement: $2:$1
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_port
                - __meta_kubernetes_pod_ip
              target_label: __address__
            - action: labelmap
              regex: __meta_kubernetes_pod_annotation_prometheus_io_param_(.+)
              replacement: __param_$1
            - action: labelmap
              regex: __meta_kubernetes_pod_label_(.+)
            - action: replace
              source_labels:
                - __meta_kubernetes_namespace
              target_label: namespace
            - action: replace
              source_labels:
                - __meta_kubernetes_pod_name
              target_label: pod
            - action: drop
              regex: Pending|Succeeded|Failed|Completed
              source_labels:
                - __meta_kubernetes_pod_phase
            - action: replace
              source_labels:
                - __meta_kubernetes_pod_node_name
              target_label: node

        - job_name: kubernetes-pods-slow
          honor_labels: true
          kubernetes_sd_configs:
            - role: pod
          relabel_configs:
            - action: keep
              regex: true
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_scrape_slow
            - action: replace
              regex: (https?)
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_scheme
              target_label: __scheme__
            - action: replace
              regex: (.+)
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_path
              target_label: __metrics_path__
            - action: replace
              regex: (\d+);(([A-Fa-f0-9]{1,4}::?){1,7}[A-Fa-f0-9]{1,4})
              replacement: "[$2]:$1"
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_port
                - __meta_kubernetes_pod_ip
              target_label: __address__
            - action: replace
              regex: (\d+);((([0-9]+?)(\.|$)){4})
              replacement: $2:$1
              source_labels:
                - __meta_kubernetes_pod_annotation_prometheus_io_port
                - __meta_kubernetes_pod_ip
              target_label: __address__
            - action: labelmap
              regex: __meta_kubernetes_pod_annotation_prometheus_io_param_(.+)
              replacement: __param_$1
            - action: labelmap
              regex: __meta_kubernetes_pod_label_(.+)
            - action: replace
              source_labels:
                - __meta_kubernetes_namespace
              target_label: namespace
            - action: replace
              source_labels:
                - __meta_kubernetes_pod_name
              target_label: pod
            - action: drop
              regex: Pending|Succeeded|Failed|Completed
              source_labels:
                - __meta_kubernetes_pod_phase
            - action: replace
              source_labels:
                - __meta_kubernetes_pod_node_name
              target_label: node
          scrape_interval: 5m
          scrape_timeout: 30s

        # This is the job that picks up kube-state-metrics and node-exporter:
        # both charts annotate their own Service with prometheus.io/scrape=true.
        # They are separate Helm releases now (they used to be subcharts of the
        # prometheus release) — discovery does not care, which is why no
        # install-order constraint exists between them and this release.
        - job_name: kubernetes-service-endpoints
          honor_labels: true
          kubernetes_sd_configs:
            - role: endpointslice
          relabel_configs:
            - action: keep
              regex: true
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_scrape
            - action: drop
              regex: true
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_scrape_slow
            # ┌────────────────────────────────────────────────────────────────┐
            # │ DEDUP. Without this, every MULTI-PORT Service annotated        │
            # │ prometheus.io/scrape=true produces one target PER endpointslice │
            # │ port, and the __address__ rewrite three rules below collapses   │
            # │ all of them onto the single prometheus.io/port — so N identical │
            # │ targets, which VictoriaMetrics reports as duplicate-target      │
            # │ errors and then scrapes N times.                                │
            # └────────────────────────────────────────────────────────────────┘
            # Enumerated from the live cluster, not guessed — exactly three
            # Services carry a prometheus.io annotation at all:
            #   kube-system/kube-dns  53/UDP + 53/TCP + 9153/TCP,
            #                         prometheus.io/port: "9153"
            #                         -> THE offender: 3 targets, all :9153
            #   kube-state-metrics    8080 only, and NO prometheus.io/port
            #   node-exporter         9100 only, and NO prometheus.io/port
            #
            # Traefik and the three cert-manager pods are also scraped, but via
            # POD annotations through the `kubernetes-pods` job above — the pod
            # role yields one target per pod regardless of how many ports the
            # pod has, so that job never had this bug and needs no equivalent.
            #
            # `keep_if_equal` is a VictoriaMetrics relabeling extension, and it
            # is the only way to express this: the plain-Prometheus spelling
            # would be `regex: (\d+);\1`, and RE2 has no backreferences.
            #
            # THE `if` GUARD IS LOAD-BEARING, not decoration, and the live
            # annotation set above is why: kube-state-metrics and node-exporter
            # set scrape=true WITHOUT a prometheus.io/port. For them the
            # annotation label is empty, which is never equal to a port number,
            # so an UNGUARDED keep_if_equal drops both targets — that is every
            # kube_* and node_* series, i.e. both Grafana dashboards, gone. With
            # the guard the rule applies only to Services that actually pin a
            # port, which today means kube-dns alone.
            #
            # Verified against victoria-metrics v1.148.0 via
            # /metric-relabel-debug: (9153,9153) kept, (53,9153) dropped,
            # (8080, no annotation) kept.
            - action: keep_if_equal
              if: '{__meta_kubernetes_service_annotation_prometheus_io_port=~".+"}'
              source_labels:
                - __meta_kubernetes_endpointslice_port
                - __meta_kubernetes_service_annotation_prometheus_io_port
            - action: replace
              regex: (https?)
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_scheme
              target_label: __scheme__
            - action: replace
              regex: (.+)
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_path
              target_label: __metrics_path__
            - action: replace
              regex: (.+?)(?::\d+)?;(\d+)
              replacement: $1:$2
              source_labels:
                - __address__
                - __meta_kubernetes_service_annotation_prometheus_io_port
              target_label: __address__
            - action: labelmap
              regex: __meta_kubernetes_service_annotation_prometheus_io_param_(.+)
              replacement: __param_$1
            - action: labelmap
              regex: __meta_kubernetes_service_label_(.+)
            - action: replace
              source_labels:
                - __meta_kubernetes_namespace
              target_label: namespace
            - action: replace
              source_labels:
                - __meta_kubernetes_service_name
              target_label: service
            - action: replace
              source_labels:
                - __meta_kubernetes_pod_node_name
              target_label: node

        - job_name: kubernetes-service-endpoints-slow
          honor_labels: true
          kubernetes_sd_configs:
            - role: endpointslice
          relabel_configs:
            - action: keep
              regex: true
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_scrape_slow
            # Same dedup as the fast job above — see the boxed comment there for
            # why it exists and why the `if` guard is not optional. No Service
            # sets prometheus.io/scrape_slow today, so this has no live effect;
            # it is here so the bug does not come back with the first one that
            # does.
            - action: keep_if_equal
              if: '{__meta_kubernetes_service_annotation_prometheus_io_port=~".+"}'
              source_labels:
                - __meta_kubernetes_endpointslice_port
                - __meta_kubernetes_service_annotation_prometheus_io_port
            - action: replace
              regex: (https?)
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_scheme
              target_label: __scheme__
            - action: replace
              regex: (.+)
              source_labels:
                - __meta_kubernetes_service_annotation_prometheus_io_path
              target_label: __metrics_path__
            - action: replace
              regex: (.+?)(?::\d+)?;(\d+)
              replacement: $1:$2
              source_labels:
                - __address__
                - __meta_kubernetes_service_annotation_prometheus_io_port
              target_label: __address__
            - action: labelmap
              regex: __meta_kubernetes_service_annotation_prometheus_io_param_(.+)
              replacement: __param_$1
            - action: labelmap
              regex: __meta_kubernetes_service_label_(.+)
            - action: replace
              source_labels:
                - __meta_kubernetes_namespace
              target_label: namespace
            - action: replace
              source_labels:
                - __meta_kubernetes_service_name
              target_label: service
            - action: replace
              source_labels:
                - __meta_kubernetes_pod_node_name
              target_label: node
          scrape_interval: 5m
          scrape_timeout: 30s
