---
# Landing Page ConfigMap with HTML content
apiVersion: v1
kind: ConfigMap
metadata:
  name: default-landing-page
  namespace: default
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <title>🚀 Jterrazz Infrastructure</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <style>
            body {
                font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif;
                margin: 0;
                padding: 0;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                min-height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
            }
            .container {
                text-align: center;
                max-width: 600px;
                padding: 2rem;
            }
            h1 { font-size: 3rem; margin-bottom: 1rem; }
            .subtitle { font-size: 1.2rem; margin-bottom: 2rem; opacity: 0.9; }
            .services { display: grid; gap: 1rem; margin: 2rem 0; }
            .service {
                background: rgba(255,255,255,0.1);
                padding: 1rem;
                border-radius: 12px;
                backdrop-filter: blur(10px);
                transition: transform 0.2s;
            }
            .service:hover { transform: translateY(-2px); }
            .service a {
                color: white;
                text-decoration: none;
                font-weight: 500;
            }
            .status {
                background: rgba(0,255,0,0.2);
                padding: 0.5rem 1rem;
                border-radius: 20px;
                display: inline-block;
                margin: 1rem 0;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>🚀 Jterrazz Infrastructure</h1>
            <p class="subtitle">Local Development Environment</p>
            
            <div class="status">✅ Infrastructure Online</div>
            
            <div class="services">
                <div class="service">
                    <h3>🔄 ArgoCD</h3>
                    <p>GitOps continuous deployment</p>
                    <a href="https://argocd.local">Access ArgoCD →</a>
                </div>
                
                <div class="service">
                    <h3>🐳 Portainer</h3>
                    <p>Kubernetes management UI</p>
                    <a href="https://portainer.local">Access Portainer →</a>
                </div>
            </div>
            
            <div style="margin-top: 2rem; opacity: 0.7; font-size: 0.9rem;">
                <p>📊 Stack: k3s + ArgoCD + Traefik + Local SSL</p>
                <p>🏠 Local Development with .local domains</p>
            </div>
        </div>
    </body>
    </html>

---
# Simple Landing Page for Local Development
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-landing
  namespace: default
  labels:
    app: nginx-landing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-landing
  template:
    metadata:
      labels:
        app: nginx-landing
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          volumeMounts:
            - name: landing-page-content
              mountPath: /usr/share/nginx/html
      volumes:
        - name: landing-page-content
          configMap:
            name: default-landing-page

---
# Service for Landing Page
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default
  labels:
    app: nginx-landing
spec:
  selector:
    app: nginx-landing
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
