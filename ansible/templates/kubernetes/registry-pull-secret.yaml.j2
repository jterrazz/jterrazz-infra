# Registry pull secret for app namespaces
# Now managed by the Helm app chart (registry-secret.yaml template).
# App CI passes --set registry.username/password to helm upgrade.
# This file is kept as a no-op for backward compatibility with the Ansible task.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-migration-marker
  namespace: default
data:
  note: "Registry secrets are now managed by the Helm app chart"
