# Registry pull secret for app namespaces
# Allows pods to pull from registry.jterrazz.com
# Password injected by Ansible from Pulumi
# Note: We create the namespace here to ensure it exists before ArgoCD syncs

---
apiVersion: v1
kind: Namespace
metadata:
  name: app-signews-api-prod

---
apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  namespace: app-signews-api-prod
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "registry.jterrazz.com": {
          "username": "deploy",
          "password": "{{ registry_password }}"
        }
      }
    }
