---
# Notified by tasks/coredns.yml, which flushes handlers immediately
# afterwards — the rest of the role needs the new records.
- name: Restart CoreDNS
  ansible.builtin.command: kubectl -n kube-system rollout restart deploy/coredns
  environment:
    KUBECONFIG: "{{ kubeconfig }}"
  changed_when: true
