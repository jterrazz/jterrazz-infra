---
# tasks/coredns.yml flushes handlers immediately after notifying this: the rest
# of the role needs the new records before it runs.
- name: Restart CoreDNS
  ansible.builtin.command: kubectl -n kube-system rollout restart deploy/coredns
  environment:
    KUBECONFIG: "{{ kubeconfig }}"
  changed_when: true
