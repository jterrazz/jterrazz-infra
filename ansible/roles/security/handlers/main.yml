---
# Security Role Handlers

# ssh.service, not ssh.socket: base.yml turns socket activation off so the
# daemon owns the listener and a restart actually re-binds it.
- name: Restart SSH
  ansible.builtin.systemd_service:
    name: ssh.service
    state: restarted
