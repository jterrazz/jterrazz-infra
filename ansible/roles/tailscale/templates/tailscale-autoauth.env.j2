# Managed by Ansible (tailscale role). Root-only (0600) — it holds the OAuth
# client secret. Sourced by tailscale-autoauth.service.
TS_CLIENT_ID={{ tailscale_oauth_client_id }}
TS_CLIENT_SECRET={{ tailscale_oauth_client_secret }}
