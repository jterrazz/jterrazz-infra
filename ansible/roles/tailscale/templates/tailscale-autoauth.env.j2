# Managed by Ansible (tailscale role). Root-only (0600). Sourced by
# tailscale-autoauth.service so the node can self-heal a logged-out
# Tailscale on boot without a redeploy.
TS_CLIENT_ID={{ tailscale_oauth_client_id }}
TS_CLIENT_SECRET={{ tailscale_oauth_client_secret }}
