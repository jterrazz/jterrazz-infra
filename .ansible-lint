---
# Ansible-lint configuration

# Only lint ansible directory, exclude problematic files
exclude_paths:
  - .github/
  - terraform/
  - scripts/
  - local-data/
  - ansible/roles/security/tasks/main.yml  # UFW module not available in lint environment

# Enable offline mode to avoid downloading external schemas
offline: true
